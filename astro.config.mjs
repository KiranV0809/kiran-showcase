import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://kiranv0809.github.io',
  base: '/kiran-showcase/',
  output: 'static',
  devToolbar: { enabled: false },
});
